{
  "name": "@cloudflare/agents-next-websockets-example",
  "description": "Next: the WebSockets capability installed on a plain Durable Object — hibernating connections, per-connection state, tags, and Cap'n Web callables",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "types": "wrangler types env.d.ts --include-runtime false",
    "test": "vitest --run --config src/tests/vitest.config.ts"
  },
  "dependencies": {
    "agents": "*",
    "capnweb": "^0.12.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.19.1",
    "@cloudflare/workers-types": "^5.20260729.1",
    "typescript": "^6.0.3",
    "vitest": "4.1.10",
    "wrangler": "^4.115.0"
  }
}
