# Codemode MCP OpenAPI

Create `search` + `execute` MCP tools from an OpenAPI spec using `openApiMcpServer`.

Uses the Cloudflare API OpenAPI spec. Pass a Bearer token via the Authorization header — it's used for API calls but never enters the sandbox.
