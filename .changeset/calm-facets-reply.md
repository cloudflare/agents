---
"agents": patch
---

Route asynchronous callable and streaming responses through their originating facet WebSocket frame, isolate concurrent facet broadcast bridges, and preserve explicit `StreamingResponse` connection targets.
