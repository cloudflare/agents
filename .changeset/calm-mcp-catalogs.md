---
"agents": patch
"@cloudflare/think": patch
---

Cache MCP JSON Schema conversion for unchanged connection catalogs, and let Think agents disable automatic MCP AI-tool exposure when MCP is available through a transport-only connector.
