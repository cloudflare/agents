---
"agents": minor
---

Graduate the Session API to the stable `agents/sessions` entry point. The previous `agents/experimental/memory/session` path remains available as a deprecated compatibility re-export.
