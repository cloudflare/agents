---
"@cloudflare/agents-voice-elevenlabs": minor
---

New package: ElevenLabs TTS provider for the Agents voice pipeline. Implements `TTSProvider` from `agents/voice` with the ElevenLabs text-to-speech API. Configurable voice, model, and output format. Use by overriding `synthesize()` on your `VoiceAgent` subclass.
