---
"agents": minor
---

Add workspace mixin (`withWorkspace`) — durable file storage for any Agent with hybrid SQLite+R2 backend and optional just-bash shell execution. Import from `agents/workspace`.
