---
"agents": minor
---

Add experimental `agents/driver`, `agents/pi`, and `agents/opencode` entrypoints for alarm-driven durable harness execution, durable tool runs, cancellation recovery, stream replay, and native Pi and OpenCode adapters.
