---
"agents": minor
---

Add experimental browser CDP tools for the Agents SDK, using the live Browser Rendering protocol instead of bundling the spec and covering the local browser-binding flow with Wrangler-backed end-to-end tests.
