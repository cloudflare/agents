---
"@cloudflare/worker-bundler": patch
---

Bump `es-module-lexer` from `^2.0.0` to `^2.1.0`. Caret upper bound (`<3.0.0`) is unchanged.

No API or runtime behavior change in `@cloudflare/worker-bundler` itself.
