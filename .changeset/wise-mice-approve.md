---
"@cloudflare/codemode": minor
---

Add framework-neutral `execute`, `search`, and `describe` methods to the durable Code Mode runtime handle so MCP servers and other non-AI-SDK hosts can use its execution and discovery surfaces directly. Search and describe results now identify methods that require approval.
