---
"@cloudflare/think": minor
---

Add `@cloudflare/think/workspace-shell`, an opt-in Computer Worker Shell workspace that exposes command execution as `bash` in regular turns and as `workspace.bash()` in codemode.
