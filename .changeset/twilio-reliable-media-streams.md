---
"@cloudflare/voice-twilio": patch
---

Fix Twilio Media Streams calls failing to connect or play audio. Durable Object WebSocket upgrades now keep their HTTP(S) URL, and agent audio works with both `Blob` and `ArrayBuffer` delivery.
