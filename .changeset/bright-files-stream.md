---
"@cloudflare/shell": minor
---

Expose `writeFileBytes` through `WorkspaceFsLike` so custom and remote workspaces can receive binary Agent Skill resources without requiring the full `Workspace` class.
