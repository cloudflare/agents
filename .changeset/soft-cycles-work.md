---
"@cloudflare/codemode": patch
---

Adapt streamed AI SDK tools through `ToolSetConnector`, preserve their output
schemas, pass stable per-call execution options and timeout cancellation,
support declarative Code Mode approval/replay policies, and include callable
signatures in method-level discovery.
