---
"agents": patch
---

Add optional `MCPServerFilter` parameter to `getAITools()`, `listTools()`, `listPrompts()`, `listResources()`, and `listResourceTemplates()` for scoping results to specific MCP servers by ID, name, or connection state.
