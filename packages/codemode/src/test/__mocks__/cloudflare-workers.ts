/**
 * Mock for cloudflare:workers used in vitest (Node environment).
 * Provides a minimal RpcTarget base class so ToolDispatcher can extend it.
 */
export class RpcTarget {}
export class WorkerEntrypoint {}
