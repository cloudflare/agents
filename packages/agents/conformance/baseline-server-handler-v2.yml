# Expected failures for the SDK v2 createMcpHandler endpoint at 2026-07-28.
# Kept separate from every legacy baseline because scenario names and protocol
# semantics differ. Remove entries as they start passing; stale entries fail CI.
server:
  # Host/Origin validation is an explicit upstream security middleware choice;
  # Workers deployments sit behind Cloudflare host validation and do not enable
  # the SDK's localhost-only defaults.
  - dns-rebinding-protection

  # @modelcontextprotocol/server@2.0.0-beta.2 does not strip HTTP OWS before
  # standard-header comparison on runtimes that expose it through Headers.get.
  # Fixed upstream by modelcontextprotocol/typescript-sdk#2453; remove this
  # entry when consuming the published beta containing that fix.
  - http-header-validation

  # The exact beta does not yet implement the optional tasks extension. These
  # extension scenarios are selected only by the alpha conformance `all` suite.
  - tasks-lifecycle
  - tasks-capability-negotiation
  - tasks-wire-fields
  - tasks-request-state-removal
  - tasks-mrtr-input
  - tasks-request-headers
  - tasks-dispatch-and-envelope
  - tasks-required-task-error
  - tasks-mrtr-composition
