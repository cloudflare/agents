node_modules/
.wrangler/
dist/
worker-configuration.d.ts
.dev.vars
.env
cloudflare-warp-ca.pem

# Host-specific WARP / corporate-MITM CA bundle dropped into ca/ for
# builds behind a TLS-inspecting proxy. Never committed; ca/.keep is
# kept so the Dockerfile COPY always has a source.
ca/*.crt
!ca/.keep
